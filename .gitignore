# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies & build output
node_modules
dist
dist-ssr
*.local

# TypeScript incremental build cache
*.tsbuildinfo

# Environment variables (may contain API keys)
.env
.env.local
.env.*.local

# Tauri updater signing key (NEVER commit the private key)
src-tauri/tauri.key

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude / Cursor / agent tooling (local-only)
.claude/
.cursor/
CLAUDE.md

# Images source files (design working files)
src/assets/.sourcefiles

# Internal superpowers plans/specs (development-only)
docs/superpowers/

# MimoCode
.mimocode

# Zcode
.zcode

# workbuddy
.workbuddy

# Python virtual env accidentally dropped in project root
.agent-reach-venv/

# Temp notes
docs/.note

# Generated third-party license attributions (regenerated by build)
public/THIRD-PARTY-LICENSES
src/generated/

# OS / temp files
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.tmp
*.bak
~$*
